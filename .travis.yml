language: minimal

services:
  - docker

before_install:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

script:
  - docker build -t $DOCKER_USERNAME/code-publisher .

deploy:
  provider: script
  script: docker push $DOCKER_USERNAME/code-publisher
  on:
    branch: main
